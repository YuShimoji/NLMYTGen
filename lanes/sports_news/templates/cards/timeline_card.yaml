card_type: timeline_card
purpose: "Show event order with source-backed beats."
required_fields:
  - timeline_title
  - beats
beat_required_fields:
  - label
  - time_or_order
  - fact_ref
visual_rules:
  max_beats: 5
  beat_visual_source: text_and_geometric_markers
notes:
  - "Use geometric markers and text as the default event-order view."
