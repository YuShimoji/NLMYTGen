# Development Generated Video Challenge

## Current challenge identity

- Challenge ID: `DEVELOPMENT_GENERATED_VIDEO_CHALLENGE-20260813-NLMYTGEN-001`
- State: `inputs_live_verified_renderer_unavailable`
- Review candidate: none
- Rendered MP4: none

This is a distinct development challenge created under the policy that an
unavailable prior review video may be followed by a new challenge selected by
the Coordinator, Supervisor, or user. It is not a reconstruction, successor,
or substitute for either of these earlier identities:

- the prepared 1,777-frame YMMP whose SHA-256 is
  `576b2e911b5ab0d3ee213fa6fe886994b5cd89e26589a561df15d583689e3105`;
- the older proof identified in its handoff as `3fd5...d288`.

Neither earlier identity may be attached to an artifact produced for this
challenge. No review packet may be issued until a new, real MP4 exists.

## Bounded outcome

The development target is one non-overwriting, locally reviewable 20--35
second MP4 produced by YMM4. It must contain 6--10 spoken/subtitle cues, two
speaker identities, at least three content-linked native visual changes,
readable speaker distinction, and the proven outline treatment where the
operator-saved template permits it. Python remains glue only: it must not
generate audio or video, create a YMMP from zero, or replace YMM4 as the
production basis. Generated SVG, a newly invented topic, and a new major
visual method are outside this challenge.

## Locally confirmed input lineage

The currently selected source lineage is the already existing new-banknote
NotebookLM-derived package at:

`production_pilots/yukkuri_newsroom_content_spine_002/external_editorial_input/new_banknote_security_notebooklm_001/`

The following existing files are the authority to recheck when the executor
is restored:

- `canonical_script.json` and `canonical_script.txt` for the nine-cue source;
- `human_script_approval_receipt.json` for the approved wording, ordering, and
  scene allocation;
- `derived_yymm4_import.csv` for the existing Reimu/Marisa import rows;
- `auto_video_pipeline/new_banknote_real_media_episode_manifest.json` for the
  prior content-linked visual mapping.

This selects the source lineage, not a shortened script. The confirmed prior
full cut is about 73.583333 seconds, so importing it unchanged would violate
the 20--35 second challenge bound. Choosing or omitting cues would be a
semantic selection. The Coordinator is authorized to select the shortest
coherent contiguous 6--10 cue span as a non-canonical development excerpt,
without rewriting or reordering cues. That selection does not alter the
approved full script, topic, canon, editorial winner, or any prior artifact
identity. No exact excerpt has been materialized yet, and the approved full
script remains unchanged.

The source lineage and carrier were re-read from a functioning local executor:

| Input | Bytes | SHA-256 |
| --- | ---: | --- |
| `canonical_script.json` | 11,449 | `4d272900e84c8f87c484aa84c1dd1909207ee8acc189603009a186af65837c47` |
| `canonical_script.txt` | 1,350 | `4eff43d0cd1f7842b02aaacd8ac6393cc12910fe70f21d650d4a31c74c17c091` |
| `human_script_approval_receipt.json` | 2,762 | `1ac490cbcd06483082b2d56a97ff758199ef37ab4b6d1229806c24b8698d7e0f` |
| `derived_yymm4_import.csv` | 1,431 | `127dd3edd32ce6131f339819263a6d2716570f800ad212b0741a384b7e19f9ee` |
| `auto_video_pipeline/new_banknote_real_media_episode_manifest.json` | 12,514 | `25ac9df0a9af1c0170c88b2e92fef34ff7278934f7d5570e87e4007d44ab46f1` |
| `samples/palette.ymmp` | 143,264 | `b608adeb2bf567302b9b0d67b1f3882d72525117d6a1c4f7b4d95d5f45b0c56b` |

The operator-saved style carrier currently identified for reuse assessment is
`samples/palette.ymmp`. It contains the distinct character identities
`ゆっくり霊夢赤縁` and `ゆっくり魔理沙黄縁` and existing outline treatment,
but it is not itself a challenge proof and must not be described as one. Its
absolute media references and current timeline contents must be revalidated
locally before any patch.

The repository-local automation actuator is
`tools/Ymm4RenderAutomation/bin/Release/net10.0-windows/Ymm4RenderAutomation.exe`.
Its existence does not make the route executable: no permissible
`YukkuriMovieMaker.exe` was found in the locally inspected repository,
worktrees, PATH, or registered install locations. Download, installation, EULA
acceptance, credential use, audible preview, and shared GUI manipulation are
not authorized by this challenge.

## Exact executor and renderer boundaries

The originating execution lane could not start even a read-only PowerShell
process. The minimal
probe

```powershell
Get-Location
```

failed before command execution while starting the WindowsApps `pwsh.exe`
shim, with
`CreateProcessAsUserW failed: 5 (access denied)`. This is an executor boundary,
not a repository, source, YMM4, render, or creative-acceptance result. A
separate functioning executor subsequently completed repository readback,
source/carrier hash verification, `git status`, and `git diff --check`, so the
project-wide re-entry boundary is now the missing permissible YMM4 renderer,
not PowerShell availability.

Re-enter from a lane that can start a local process against the checkout. Read
`AGENTS.md`, `docs/REPO_LOCAL_RULES.md`, this file, and `docs/runtime-state.md`,
then recheck branch, HEAD, tracked/untracked/ignored state, and the hashes above
before mutation. Select the authorized contiguous development excerpt and
materialize its exact CSV/ScriptIR/VisualIR/registry/template-patch inputs.
Render only after a permissible existing `YukkuriMovieMaker.exe` is available;
download, installation, EULA acceptance, and substitution with a Python video
renderer remain outside this challenge. Do not fabricate an MP4 or review
route while that actuator is absent.

This file intentionally contains no live task, thread, or host identifier.
