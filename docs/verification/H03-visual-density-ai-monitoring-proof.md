# Visual Density Proof

> Date: 2026-04-06
> Sample: `AI監視が追い詰める生身の労働`
> Inputs: packaging brief + cue memo + script reference

- score_version: 0.1
- video_id: amazon_ai_monitoring
- total_score: 79
- band: acceptable

## Category Scores

| Category | Score | Notes |
|---|---|---|
| `scene_variety` | 3 | S1 van interior, S2 warehouse UI, S3 road risk, S4 PR/legal contrast, S5 future city all differ clearly |
| `information_embedding` | 2 | scanner UI, timer, graph, PR/legal text are implied, but not every key number is yet guaranteed as a distinct visual beat |
| `symbolic_asset` | 3 | inhaler, hand scanner, countdown, thank-you button, drone all function as memorable motifs |
| `tempo_shift` | 2 | cold open, dense system analysis, road escalation, PR contrast, reflective ending are distinct, but the middle could still flatten if edited too uniformly |
| `pattern_balance` | 2 | the memo balances anecdote, UI explanation, and abstract thought reasonably well, though S2-S4 lean heavily toward information-heavy exposition |
| `stagnation_risk` | 2 | section turnover is good, but S2 and S4 could become visually repetitive if the operator leaves them on one screen type too long |
| `promise_visual_payoff` | 2 | `71.4%`, `タイム・オフ・タスク`, and the PR gap are supportable, but `19億ドル` and the strongest number-first promise still need deliberate visual surfacing |

## Warnings

- `VISUAL_DATA_PROMISE_UNPAID`
- `VISUAL_PATTERN_REPEAT`

## Best Supports

- inhaler incident gives the opening a concrete symbolic hook
- scanner / timer UI creates a visible monitoring system instead of abstract narration only
- thank-you button versus legal text gives S4 a clear contrast structure

## Stagnation Points

- S2 can flatten into "warehouse UI forever" if timer, scanner, and mental-pressure beats are not separated visually
- S4 can become noisy or static because PR imagery, legal text, and labor response compete for the same section
- S5 can drift into pure abstraction unless one earlier motif returns visually

## Recommended Repairs

- script / cue:
  - make `71.4%` and `タイム・オフ・タスク` separate visual beats instead of one blended explanation block
  - compress S4 into three explicit frames: `感謝ボタン` → `免責/規約` → `労組反応`
  - carry one earlier motif into S5, such as the countdown, scanner, or app button, so the ending is not visually detached
- packaging / production:
  - if the thumbnail leads with `19億ドル`, ensure that S4 has a clear, screen-filling money/PR contrast moment
  - if the thumbnail leads with `71.4%`, surface a visible numeric card or panel before the middle of the video
  - avoid letting S2 and S4 both use the same dense UI frame style for too long without a contrast cut

## Pass Conditions

| Condition | Result | Notes |
|---|---|---|
| all 7 category scores assignable | pass | no extra axes were needed |
| at least one warning maps to a clear visual repair | pass | S2 and S4 both yield concrete restructuring suggestions |
| H-01 dependency is visible | pass | visual payoff is judged against thumbnail promise and section payoff notes |
| result explains why density feels weak | pass | risk is localized to data-heavy blocks and late visual payoff, not to the whole video |

## Assessment

| Item | Result |
|---|---|
| useful enough to keep using | yes |
| strongest improvement | it separates "lots of information" from "information that will actually become visible" |
| residual drift | final YMM4 execution could still flatten S2 or S4 if the operator does not split the planned beats |
| next improvement inside repo | packaging-side definitions complete (H-02 strict GUI rerun proof passed 2026-04-06). Next frontier is direct production workflow bottleneck reduction (script quality / staging automation / visual effects) |
