# Generic visual capability delivery acceptance

## H0 acceptance

- [x] 78 relevant code, test, data and evidence paths are classified; every
  matrix/ledger evidence reference and the generic package paths are included.
- [x] Unclassified relevant paths = 0.
- [x] 38 capabilities each have one availability class, C0-C5 level, evidence,
  inputs, behavior, limitations, failures, fallback, cost/reuse fields and an
  overclaim boundary.
- [x] `proven` begins at C3; `render_proven` begins at C4; C5 = 0.
- [x] Primitive and proven-combination evidence are distinct.
- [x] Every used primitive contributes its capability/evidence requirement;
  omitted explicit cue rows cannot hide a weaker primitive capability.
- [x] The generic Scene IR has variable scenes/cues and data-resolved recipes,
  primitives, capabilities, timing, motion request, rights and fallback fields.
- [x] Generic core contains no current-topic payload or fixed pilot counts.
- [x] Inspection, process and comparison fixtures pass the same validator.
- [x] Route A is data-only and its source packet remains unmodified.
- [x] A minimum-cost generic stack is derived from evidence.
- [x] Can / conditional / cannot / unknown and expensive/not-worth-building
  boundaries are explicit.
- [x] Static conformance does not claim YMM4 runtime behavior or C5.
- [x] Composition `evidence_level` is the weakest required-primitive capability
  (currently C0 because subtitle layout is unknown); C2 static conformance is
  tracked separately.
- [x] HTML is secondary, self-contained and table/dependency-spine based.

## Delivered state, not delivered runtime

The deliverable is an evidence-graded capability envelope and composition
grammar. It authorizes no project generation, YMM4 action, render, publication,
rights decision or master integration.

## H1 decision gate

Review the capability matrix and choose at most one small generic capability
probe. Good candidates are a short Text/Image/Shape layout check, one fade, or
one bounded transform that materially affects multiple archetypes. The probe
must not become a final topic scene.

## What would close the next evidence gaps

- C3: actual YMM4 observation of the selected generic primitive and its fallback.
- C4: retained render plus media structure/probe/decode verification, only when
  render semantics matter.
- C5: a second heterogeneous real episode through the same generic core without
  core-code changes.
